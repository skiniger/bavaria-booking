# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True

# Database
DB_NAME=gaststaette_db
DB_USER=admin
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# Für lokale Entwicklung mit SQLite (alternativ zu PostgreSQL):
# Kommentieren Sie die obigen DB-Einstellungen aus und verwenden Sie SQLite in settings.py
